# AGENTS.md

이 저장소에서 작업하는 에이전트가 **작업을 시작하기 전에** 읽는 지침입니다.

## 작업 전에 반드시 읽을 것

1. [design-rules.md](design-rules.md) — 시각·문구 규칙과 금지 목록
2. [design-tokens.css](design-tokens.css) — 색·타이포·간격·모션의 확정 값

## 반드시 지킬 것

- **값은 토큰 변수로만 참조합니다.** `#0c74ee` 같은 리터럴을 쓰지 않고 `var(--signal)`을 씁니다.
- **토큰에 없는 값이 필요하면 만들지 말고 멈춥니다.** PR 설명에 "토큰 추가가 필요하다"고 적고 그대로 둡니다.
- **`main`에 직접 커밋하지 않습니다.** 항상 작업 브랜치에서 작업합니다.
- **`design-tokens.css`를 수정하지 않습니다.** 이 파일은 brand-kb에서 내려온 사본입니다.
- **아이콘은 [Lucide](https://lucide.dev)에서 가져옵니다.** path를 직접 그리지 않습니다.
- **요청받지 않은 것을 바꾸지 않습니다.** 티켓에 적힌 범위만 수정합니다.

## 작업이 끝나면

- 변경한 파일과 이유를 PR 설명에 적습니다.
- [design-rules.md](design-rules.md)의 리뷰 체크리스트를 확인하고, 통과하지 못한 항목이 있으면 PR 설명에 밝힙니다.
- 판단이 필요한 지점이 있었다면 무엇을 선택했고 왜 그랬는지 한 줄로 적습니다.

## 이 저장소의 구조

```
index.html          단일 랜딩 페이지
styles.css          페이지 스타일 — 토큰을 소비
design-tokens.css   값의 원천 (수정 금지)
design-rules.md     판단 기준
```

## 하지 말아야 할 것

- 빌드 도구나 프레임워크를 추가하지 않습니다. 이 저장소는 정적 파일 3개로 유지합니다.
- 새 의존성을 설치하지 않습니다.
- 파일을 새로 만들기 전에, 기존 파일에서 해결되는지 먼저 봅니다.
