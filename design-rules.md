---
title: Design Rules
kb_type: reference
owner: 브랜드·콘텐츠
last_reviewed: 2026-08-10
tags: [design, rules, tokens, agent]
---

# Design Rules

화면을 만들 때 지키는 규칙입니다. 사람이 리뷰할 때의 기준이자, 에이전트가 작업 전에 읽는 입력입니다.
값은 [design-tokens.css](design-tokens.css)에 있습니다. 이 문서는 **값으로 표현할 수 없는 판단**만 담습니다.

## 원칙 다섯 줄

1. 기본은 무채색과 여백입니다. 색은 의미가 있을 때만 씁니다.
2. 위계는 크기 대비가 아니라 여백과 순서로 만듭니다.
3. 장식은 정보가 아닙니다. 빼도 뜻이 전달되면 뺍니다.
4. 사람이 읽는 문장을 씁니다. 홍보 문구가 아니라 사실을 적습니다.
5. 움직임은 등장 한 번으로 끝냅니다.

## 하지 않는 것 — 시각

| 금지 | 이유 |
|---|---|
| 토큰에 없는 색 값 | 원천이 갈라집니다 |
| 채도색 2개 이상 | Signal 한 색만 씁니다 |
| 그라데이션 배경, 글로우, 네온, 블러 | 정보가 아니라 분위기입니다 |
| 이모지를 아이콘 대신 사용 | 아이콘은 [Lucide](https://lucide.dev)에서 가져옵니다 |
| 손으로 그린 아이콘 path | 시안과 어긋납니다 |
| 8의 배수가 아닌 간격 | 스케일이 무너집니다 |
| 정의되지 않은 radius | 3단계만 씁니다 |
| 스크롤에 반응하는 지속 애니메이션 | 읽기를 방해합니다 |
| 의미 없이 반복되는 3열 카드 | 템플릿처럼 보입니다 |
| 그림자로 만드는 깊이 | 경계는 선으로 만듭니다 |

## 하지 않는 것 — 문구

| 금지 | 대신 |
|---|---|
| 혁신적인 · 차세대 · 강력한 · 스마트한 | 무엇을 하는지 그대로 |
| 손쉽게 · 단 몇 초 만에 · 누구나 | 실제 단계나 조건 |
| 출처 없는 수치 | 출처와 기준일이 있는 수치만 |
| 단위가 섞인 수치 | 한 화면에서 단위를 통일 |
| 브랜드 문서의 시그니처 표현 복붙 | 그 자리에 맞는 자연스러운 문장 |
| 근거 없는 최상급 | 비교 기준을 밝히거나 삭제 |

문장이 길어지면 잘라내는 쪽을 먼저 봅니다. 설명을 더 붙여야 이해되는 화면은 대개 구조가 잘못된 것입니다.

## 값은 토큰만

- 색·간격·radius·타이포·easing은 **토큰 변수로만** 참조합니다. 리터럴 값을 쓰지 않습니다.
- 토큰에 없는 값이 필요하면, 화면에서 만들지 말고 토큰을 추가할지부터 논의합니다.
- 제품 고유 색(예: 지수 패밀리 구분색)은 해당 저장소에서 정의하고, 공용 토큰에 넣지 않습니다.

## 컴포넌트

- 기존 컴포넌트를 조합해서 만듭니다. 비슷한 것을 새로 만들지 않습니다.
- 없어서 새로 만들었다면 스토리를 함께 만듭니다. 앱이 import 하는 코드를 스토리가 그대로 씁니다.
- 랜딩은 섹션 블록 단위, 앱은 원자 단위로 나눕니다.

## 리뷰 체크리스트

PR을 열기 전에 확인합니다.

- [ ] 토큰 밖의 색·간격 값이 없다
- [ ] 채도색이 화면당 1~2곳을 넘지 않는다
- [ ] 아이콘이 전부 Lucide다
- [ ] 빼도 뜻이 전달되는 장식이 없다
- [ ] 홍보 문구가 아니라 사실을 적었다
- [ ] 수치에 출처와 기준일이 있다
- [ ] 등장 애니메이션이 1회로 끝난다
- [ ] 375px에서 가로 스크롤이 없다

## 기계가 잡는 것과 사람이 보는 것

| 기계가 잡습니다 | 사람이 봅니다 |
|---|---|
| 토큰 밖 색·간격 값 | 문장의 톤이 맞는가 |
| 정의되지 않은 radius | 위계가 읽히는가 |
| 새로 만든 컴포넌트 | 이 구성이 설득력이 있는가 |
| 대비 기준 미달 | 전체 인상이 맞는가 |

왼쪽은 PR 단계에서 자동으로 걸러냅니다. 리뷰에서는 오른쪽만 봅니다.

## 열린 결정

- 데이터 방향색(상승·하락) — 그린/레드와 레드/블루 두 기록이 어긋나 있어 미확정
